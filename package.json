{
  "name": "ntu-med-god",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently — kill-others \"yarn run backend\" \"yarn run frontend\"",
    "backend": "cd backend && yarn run dev",
    "frontend": "cd frontend && yarn start",
    "build": "cd frontend && yarn run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0"
  }
}
